<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lk.bookshoppadminapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookShoppAdminApp">
        <activity android:name=".UI.AdminLoging">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.UpdateActivity" />
        <activity android:name=".UI.DeleteProductActivity" />
        <activity android:name=".UI.UpdateProductActivity" />
        <activity android:name=".UI.AdminProfileActivity" />
        <activity android:name=".UI.ViewProductActivity" />
        <activity android:name=".UI.LoadAllProduct" />
        <activity android:name=".UI.AdminRegister" />
        <activity android:name=".UI.AddProductActivity" />
        <activity android:name=".UI.ViewOrders" />
        <activity android:name=".UI.ReportActivity" />
        <activity android:name=".UI.RidersActivity" />
        <activity android:name=".UI.ProductActivity" />
        <activity android:name=".UI.OrdersActivity" />
        <activity android:name=".UI.AdminHome">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>